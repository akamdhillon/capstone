# =============================================================================
# CLARITY+ SMART MIRROR - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and modify as needed for your environment
# =============================================================================

# -----------------------------------------------------------------------------
# SYSTEM TOGGLES
# -----------------------------------------------------------------------------

# Enable/disable thermal sensor functionality
# Set to 'true' when thermal hardware is connected
THERMAL_ENABLED=false

# Development mode - uses video files instead of live camera
DEV_MODE=true

# Logging verbosity: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Node environment for frontend
NODE_ENV=production

# -----------------------------------------------------------------------------
# NETWORK CONFIGURATION
# -----------------------------------------------------------------------------

# Physical node IP addresses (Ethernet bridge)
# For Mac development with Jetson via direct Ethernet, use 192.168.2.x
RPI_IP=192.168.10.1
JETSON_IP=192.168.2.2

# Camera device path (Linux)
CAMERA_DEVICE=/dev/video0

# Mac camera index (0=built-in FaceTime, 1+=USB cameras)
MAC_CAMERA_INDEX=0

# -----------------------------------------------------------------------------
# DOCKER RUNTIME
# -----------------------------------------------------------------------------

# Set to 'nvidia' on Jetson for GPU passthrough
# Set to 'runc' for development without GPU
DOCKER_RUNTIME=runc

# TensorRT model precision: FP32, FP16, INT8
MODEL_PRECISION=FP16
